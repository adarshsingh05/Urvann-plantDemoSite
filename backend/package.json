{
  "name": "urvan-plant-store-backend",
  "version": "1.0.0",
  "description": "Backend API for Urvan Plant Store",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "seed": "node seedData.js",
    "seed:atlas": "node seedAtlas.js",
    "migrate": "node migrateToAtlas.js",
    "test:connection": "node -e \"require('dotenv').config({ path: './config.env' }); const mongoose = require('mongoose'); mongoose.connect(process.env.MONGODB_URI).then(() => { console.log('✅ MongoDB Atlas connection successful!'); process.exit(0); }).catch(err => { console.error('❌ Connection failed:', err.message); process.exit(1); });\""
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^8.0.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "helmet": "^7.1.0",
    "express-rate-limit": "^7.1.5",
    "morgan": "^1.10.0",
    "mongodb": "^6.18.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "keywords": ["nodejs", "express", "mongodb", "api"],
  "author": "Urvan Plant Store",
  "license": "MIT"
}
